import React from 'react';
import { Product } from '@guitar-shop/shared-types';

type PropsTypes = {
  products: Product[];
};

function Catalog({ products: films }: PropsTypes): JSX.Element {
  return (
    <React.Fragment>

    </React.Fragment>
  );
}

export default Catalog;
